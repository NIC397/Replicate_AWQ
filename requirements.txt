# Core dependencies for AWQ replication
torch>=2.0.0  # Required for model operations and CUDA support
transformers==4.35.0  # Required for model loading and management
datasets>=2.12.0  # Required for loading wikitext2 dataset
numpy>=1.24.0  # Required for numerical operations
tqdm>=4.65.0  # Required for progress bars
accelerate>=0.20.0  # Required for efficient model loading
bitsandbytes>=0.39.0  # Required for quantization operations

# Web and API dependencies
gradio==3.35.2
gradio_client==0.2.9
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic==1.10.14

# Optional dependencies for additional features
safetensors>=0.3.1
einops>=0.6.1
scipy>=1.10.0
toml>=0.10.2
attributedict>=0.2.0
protobuf>=4.24.0

# Development dependencies
pytest>=7.3.1
black>=23.3.0
isort>=5.12.0
mypy>=1.3.0
pylint>=2.17.0 